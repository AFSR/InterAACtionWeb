name: Build Player

on:
  workflow_dispatch:
    inputs:
      sha:
        description: "InterAACtionPlayer commit SHA to build"
        required: true
        default: "c7dbdf5bff43e64595c66db9f9212d1391977c57"

permissions:
  contents: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout this repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Node 16 (Angular 11 toolchain)
        uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a # v4.4.0
        with:
          node-version: "16"

      - name: Clone InterAACtionPlayer at the requested SHA
        run: |
          set -euo pipefail
          SHA="${{ github.event.inputs.sha }}"
          git clone https://github.com/AFSR/InterAACtionPlayer.git upstream
          cd upstream
          git checkout "$SHA"
          git log -1 --pretty="%H %s"

      - name: Disable Angular service worker (portal owns the SW)
        working-directory: upstream
        run: |
          set -euo pipefail
          jq '(.. | objects | select(has("serviceWorker")) | .serviceWorker) |= false' \
            angular.json > angular.json.new
          mv angular.json.new angular.json
          ! grep -E '"serviceWorker":\s*true' angular.json

      - name: Install dependencies
        working-directory: upstream
        run: npm ci --legacy-peer-deps --no-audit --no-fund

      - name: Build Player (production, base-href /player/)
        working-directory: upstream
        run: npx ng build --prod --base-href /player/ --output-path dist

      - name: Strip leftover ngsw artefacts from dist
        working-directory: upstream
        run: |
          set -euo pipefail
          rm -f dist/ngsw.json dist/ngsw-worker.js dist/safety-worker.js dist/worker-basic.min.js

      - name: Replace apps/player/dist in the host repo
        run: |
          set -euo pipefail
          rm -rf apps/player/dist
          mkdir -p apps/player
          cp -r upstream/dist apps/player/dist
          cat > apps/player/dist/BUILD_INFO.txt <<EOF
          Built from: https://github.com/AFSR/InterAACtionPlayer
          SHA:        ${{ github.event.inputs.sha }}
          Built at:   $(date -u +"%Y-%m-%dT%H:%M:%SZ")
          Workflow:   ${{ github.workflow }} (run ${{ github.run_id }})
          EOF

      - name: Inject gaze bridge into Player index.html
        run: python3 scripts/patches/inject_gaze_bridge.py apps/player/dist

      - name: Open a pull request with the new build
        uses: peter-evans/create-pull-request@c5a7806660adbe173f04e3e038b0ccdcd758773c # v6.1.0
        with:
          branch: bot/player-${{ github.event.inputs.sha }}
          commit-message: |
            chore(player): build from ${{ github.event.inputs.sha }}
          title: "chore(player): build from ${{ github.event.inputs.sha }}"
          body: |
            Automated build of [AFSR/InterAACtionPlayer](https://github.com/AFSR/InterAACtionPlayer)
            pinned at `${{ github.event.inputs.sha }}`.

            - Angular 11 `--prod`, `--base-href /player/`
            - Service worker disabled (portal owns SW scope `/`)
            - Gaze bridge injected
            - Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          add-paths: |
            apps/player/dist
